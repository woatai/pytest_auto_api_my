flow:
  name: 下单主流程
  fail_fast: true
  tags:
    - order
    - smoke

steps:
  - use: login/login.yaml::login_success
  - use: product/product.yaml::product_list
  - use: product/product.yaml::product_detail
  - use: product/product.yaml::default_price
  - use: user/user.yaml::user_center
  - use: cart/cart.yaml::buy_now
  - use: order/order.yaml::check_shipping
  - use: user/address.yaml::get_default_address
  - use: order/order.yaml::order_confirm
